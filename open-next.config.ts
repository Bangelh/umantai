import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // You can customize caching, incremental static regeneration, etc. here later.
  // For now, we use the default recommended settings for a standard Next.js app.
});